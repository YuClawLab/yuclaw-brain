# v7 capability matrix (candidate; updated at the end of V7-005 engineering incl. the P1/P2 increments that fit — allocation labels are PROPOSED until the owner accepts them)

Columns: **implementation** (code in the candidate) · **validation** (tests/gates actually run) · **authority** (protocol/owner decision that exists) · **observed evidence** (real, non-synthetic) · **activation** (what is switched on) · **proposed allocation** (`PROPOSED_7_0` / `PROPOSED_7_1` / `OWNER_DECISION_PENDING` — nothing below is an adopted decision). A disabled stub, a draft protocol or a green synthetic test is not a completed scientific capability. Working code proposed for 7.1 remains available and is described truthfully; nothing is erased by the label.

| workstream | implementation | validation | authority | observed evidence | activation | proposed allocation |
|---|---|---|---|---|---|---|
| Outsider receipt program (engine, packet, challenge, decision, scoreboard, explain/history) | `v3/receipts/` complete classes: shared descriptor-pinned reads (`safeio`), one private-store boundary (`storage`), typed release TARGET manifest and exact-target coverage (`target`), corrections pinned to their original window and rendered CORRECTED, complete public shapes with read-back, coded public errors, keyed public receipt ids, receipt categories (replication / witness review / audit-break attempt / refusal), criterion-bound challenge resolution (allowlisted verifiers or reviewer evaluation), `receipts explain`, `receipts history`; one canonical payload for CLI, static JSON, REST and MCP | 14 adversarial + 8 journey + 6 boundary + 4 interface + 20 V3 + 13 V4 + 11 V5 tests (synthetic; one real offline frozen-bundle replay; T1/T2/T3 acceptance) | proposal only: no registration record, no designated reviewer appointment, no privacy review; categories and window policy are candidate contracts | 0 outsider receipts of any category; 0 real reviews; 1 legacy affiliated program entry (PREFIX_ONLY); exact-target coverage UNBOUND (no release target manifest delivered yet) | local/synthetic; public scoreboard shows zero/pending/UNBOUND | PROPOSED_7_0 (software); real program activation = OWNER_DECISION_PENDING (D3) |
| Gate #15 comprehension study | candidate protocol + participant script + reviewer scoring key + ONE schema (`v3/study/gate15_schema.py`), participant packet builder, private blob-bound study manifest, deterministic evaluator and gate-evidence adapter (`tools/yuclaw_gate15_study.py`) | 4 synthetic-form tests; `check-docs` consistency; packet build | UNADOPTED protocol; no applicability determination; no designated study reviewer; release-policy route = OWNER_DECISION_PENDING (D2-A / D2-B) | 0 human sessions | none | PROPOSED_7_0 (tooling/documents); gate stays MANUAL_REVIEW on current evidence |
| U150→U250→U350→U550 ladder | `v3/universe/ladder.py`: parameterized gate validation per rung on its own candidate set (identity, price history, liquidity threshold, substrate, shadow-run record, cross-sectional disclosure), explicit non-admission reasons, capacity/shadow inputs per rung (UNKNOWN when absent, never inherited), U550 path; `tools/yuclaw_ladder_readiness.py --fixture` full-ladder report | `tests/test_ladder_full_validation.py` (3) + existing ladder tests (synthetic fixtures only) | GATE 5 shadow requirement; no promotion decision; no registered window; no admission run above 350 exists | Phase-A shadow only (10 anomaly days/25; 0 promotions); U550 has no evidence of its own | fixture validation only; no live admission | PROPOSED_7_0 (validation tooling); promotion/window = OWNER_DECISION_PENDING |
| Phase-C before Phase-B | `v3/phase_c/spec.py` schema/dependency checks (populations, fresh-window start, endpoints, denominator, missing-session rule, statistic, decision rule — may be UNRESOLVED, stopping rule, controls, prior-observation disclosure, integrity reference replacing the ambiguous 'integrity threshold'); no threshold constant in runnable code; `phase_c_prospective_protocol_DRAFT.md` remains the narrative draft for Astra's designation | `tests/test_phase_c_spec.py` (2; incl. a source scan for threshold constants) | unregistered; decision rule UNRESOLVED pending architectural input | none | none (runnable only when REGISTERED) | PROPOSED_7_0 (checks); designation/registration = OWNER_DECISION_PENDING |
| Phase 6 / A2 / N_eff | `v3/phase6/a2_interface.py`: typed requirements record (S, event set, units, weights, horizon, variance basis, rule-4 disposition, incident dependencies) with per-input UNRESOLVED labels and rejection of incompatible units / unsupported variance inputs; never computes N_eff | `tests/test_a2_interface.py` (3) + N_eff guard tests | S undesignated; rule-4 decision pending | STRUCTURE_PRINTED; N_eff PENDING | none | PROPOSED_7_0 (interface); designation of S = OWNER_DECISION_PENDING (Astra) |
| Layer 2 / Form-4 accrual | existing deterministic path unchanged; fixture-backed end-to-end integration through the real `backfill_form4`/`parse_filing` with a fake HTTP layer and a disposable PostgreSQL 16 events table | `tests/test_form4_integration_pg16.py` (accession identity, amendments, event/filing/acceptance times, dedup on rerun, cursor/retry, no write on fetch/parse failure, no Layer-2 field written) + `tests/test_form4_candidate.py` | registered C6 sign gate controls Layer 2 | C6 DESCRIPTIVE; sign UNCONFIRMED | live poller unchanged; no new ingestion job | PROPOSED_7_0 (integration tests); Layer-2 activation stays gated |
| ETF class membership | `v3/universe/etf_class_candidate.py`: addendum validation with provenance and a supersedes chain, `apply_addendum` → PROPOSED classification (never REGISTERED from a payload), `classify` with NOT_APPLICABLE / BLOCKED_BY_REGISTRATION / PROPOSED_* / MEMBER distinct; registered set untouched | `tests/test_etf_addendum_path.py` (2) + ETF suite | addendum unapproved | ETF_SET empty in-hash | none | PROPOSED_7_0 (classification path); addendum adoption = OWNER_DECISION_PENDING |
| Phase-5 contribution anatomy | `v3/anatomy/contribution.py` + `tools/yuclaw_contribution_anatomy.py`: deterministic decomposition (score × confidence × weight), gated zero contributions with reasons, negative values, rounding reconciliation and per-component mismatch reporting; the published why-JSON cannot be reconciled without published weights (tool says NOT_POSSIBLE) | `tests/test_contribution_anatomy.py` (2; synthetic why-JSON) | registration pending; no new all-universe result | none | none | PROPOSED_7_0 (reader/command); registered read = OWNER_DECISION_PENDING |
| Effective-evidence-count naming | XLK label distinguishes the convenience count from the Phase-6 N_eff; V5 audit of user-facing count labels recorded privately (`raw/03_naming_audit.txt`) | render + language rail | estimand pending | — | — | PROPOSED_7_0 (audit result; no relabeling) |
| Nightly status | `v3/ops/nightly_status.py` adapter + `preview` (launch identity: launcher digest and the run's commit found read-only; NO_LOG / NO_RUN_FOUND explicit; delivery never attempted) | `tests/test_nightly_status.py` (5: success/failure/interruption/stale/missing evidence/delivery failure) | live delivery needs the owner's scoped activation (D4-NIGHTLY-STATUS) | nightlies 09-08…09-11 COMPLETED; preview of the real log recorded privately | not activated | PROPOSED_7_0 (preview); delivery activation = OWNER_DECISION_PENDING |
| Sentinel | `docs/methodology/sentinel_integration_proposal.md` (reconciles the launcher's rules with the fact that refusals are not written to its log) + `v3/ops/sentinel_lifecycle.py` pure model | `tests/test_sentinel_lifecycle.py` (3 synthetic lifecycle cases) | session rule unchanged; launcher edit (record refusals) = OWNER_DECISION_PENDING (D4-SENTINEL) | 4 launcher runs recorded; refusals unrecorded by design of the current launcher | unchanged | PROPOSED_7_0 (proposal + model); no policy change |
| Note snapshot contract | contract v3 live (unchanged default); `v3/ops/note_snapshot.py` coordinator: one REPEATABLE READ READ ONLY exporter, per-reader snapshot import before the first query, bounded lifetime, registry-file identity pinned, generated-at + snapshot-scope label, explicit errors, no LIVE_RECHECK fallback | `tests/test_note_snapshot_pg16.py` (3) on a DISPOSABLE PostgreSQL 16 instance (initdb/pg_ctl by the executor user): late commit invisible, expiry, import failure, registry drift, strict mismatch | activation = OWNER_DECISION_PENDING (D4-SNAPSHOT) | 4+ green nightlies under v3 | v3 live; coordinator not wired | PROPOSED_7_0 (coordinator, opt-in); activation pending |
| 2028 trading calendar | official NYSE 2028 full closures and 13:00 ET early closes registered with source attribution (NYSE Group announcement 2025-12-23, rechecked 2026-09-14); horizon 2026-01-01…2028-12-31; 2029 explicit unsupported | 3 calendar tests (closures, early closes, boundaries) | official publication (recorded) | — | — | PROPOSED_7_0 |
| RTX capacity / evidence-only expansion | `tools/yuclaw_capacity_assessment.py` (measured vs inferred vs unknown separated; software workload estimates; operator hours UNKNOWN; RTX NOT_COMMISSIONED; commissioning checklist) + `capacity_and_rtx_status.md` | `tests/test_capacity_assessment.py` (1) | commissioning pending; owner disposition pending | 2026-08-02 audit; single box; operator H unknown | — | PROPOSED_7_0 (tool); no workload run |
| Commercial readiness / governance | `docs/methodology/commercial_governance_pack.md` (data flow, retention state, capability, clauses cross-referenced to `API_TERMS.md` §1–§9 and the release/receipt governance, outstanding ratifications) + `commercial_capability_brief.md` | language rail | counsel review = blocker (unchanged) | none | none | PROPOSED_7_0 (documents only); no workflow activated |
| Governance and release | A-6 flow preserved; reviewed v7 publisher (private) with verification-completion record, release-policy record (allocation + Gate #15 route), evidence stage (FINAL target manifest as release evidence), idempotent resume and per-write rechecks; production transition plan; staged anchor guard active in production checkout | publisher failure-path tests on disposable repositories with fake transports; full gate battery + release-state generator on the candidate | A-6 owner sentence required; allocation acceptance (D1) and Gate #15 route (D2) = OWNER_DECISION_PENDING | — | — | Phase-1 handoff prepared; publication conditional |

Original-v7 success measure (unchanged): outsider evidence and universe scale. Actual counts at this candidate: outsider receipts 0 (all categories), independent persons/groups 0, exact-target artifact coverage UNBOUND, eligible windows 0 (no registration), shadow days 25 (Phase A, 10 label-anomaly days), operator/GPU resource use per the 2026-08-02 audit, operator H unknown. Orders, code lines and test counts are not evidence.
