# Tool versions for .github/workflows/v8-validation.yml (PIP_CONSTRAINT) — the versions the local candidate
# evidence was produced with (V8-011 packaging record). twine 7.0.0 is required: 6.x cannot parse the
# Metadata 2.5 that hatchling 1.32 writes. playwright must match the --playwright-spec the workflow passes.
build==1.4.2
hatchling==1.32.3
twine==7.0.0
playwright==1.62.0
