"""Local user profile + watchlist persistence at ~/.yuclaw/profile.json.

This is the Day 8 'Yuclaw Memory' layer. No database — a single JSON file
per user keeps the surface portable and lets the CLI run without any
server-side state.
"""
