Fictional fixture data only. A workspace journal and a research export written by the PRE-MODULE candidate build
(V8-011 wheel, yuclaw 8.0.0 before SHD, EVO, COM and PRC existed) from the packaged fixture `001_base`. Kept so that every
later build must still open this journal (the three events exactly as that build wrote them: source, claim, export) and verify this export unchanged (acceptance family X-04).
Canonical digest of the export: 564cc30e4a02fe99538941f6e76ee081fc4d80614e7d960951c055add71d279d.
