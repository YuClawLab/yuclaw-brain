# V8-014 — claim-to-evidence table (expanded 8.0.0 candidate)

Research and education only. Not investment advice. Each claim describes **observed behaviour of this candidate on a
supported configuration**; the right-hand column is its limit. Status words are never collapsed: `IMPLEMENTED` (code and
passing checks in the checkout), `INSTALLED_AND_DEMONSTRATED` (also shown by the module journey from the installed wheel and
sdist), `HOSTED_CI_VERIFIED` (**not claimed**: `REMOTE_CI_NOT_RUN_FOR_NEW_SHA`), `RELEASED` (**not claimed**).

| # | Claim (observed behaviour) | Evidence on this candidate | Limit |
|---|---|---|---|
| 1 | A bundle whose bytes changed after approval, whose approval expired or was revoked, or which its own submitter approved is refused on the protected route with a fixed code; an independently approved bundle is admitted. | `tests/test_v8_mod_shield.py` (`test_S01…`, `test_a_pre_approved…`, `test_S02…` ×2); module journey step 2 from both installed artifacts | Approval shows that an administrator's local credential signed for those bytes — not that any statement in them is true (factual adjudication is always NOT_ASSESSED). |
| 2 | Bundles are opened only inside a restricted worker that, on this host, could not read a canary secret, write anywhere, reach canary TCP/UDP endpoints, open a unix socket, start a process, signal the server or allocate 2 GiB; with no such worker the route is closed and nothing falls back. | `test_S04_S07…`, `test_S04_descriptors…`, `test_S05_closed_route…`; `isolation_support_matrix.json`; journey step 2 | Landlock + seccomp process confinement on Linux aarch64 (kernel 6.17), Python 3.12 and 3.10. bubblewrap cannot start on this host. x86-64 and the hosted runner are unverified. Not a container; no defence against a kernel flaw. **No independent security review.** Zero violations refers to the enumerated probes only. |
| 3 | Instruction-like text inside evidence has no effect: nothing ran, no file was touched, and it is shown only as escaped text to an administrator or reviewer. | `test_S06…`; journey step 2 (page title unchanged, no `<script>`, canary file absent) | One constructed payload family. It does not show that all prompt-injection or AI attacks are prevented. |
| 4 | After a **measured** change to a file inside a protocol's configured dependency closure, earlier review evidence is reported stale with the reason; evidence whose closure is untouched is reused; a file swapped after registration is not evaluated under the old identity. | `tests/test_v8_mod_evo_com.py` (`test_E02_E07…`, `test_E07…`, `test_E01_unknown…`); journey step 5; `measurements.json` (0 missed, 0 unnecessary in 12 real changes) | Local files and this interpreter only; a provider alias stays DECLARED. It audits recorded evidence; it does not detect or control self-improvement and gates no deployment. |
| 5 | A recorded failure stays open across a later pass, a new label and a renamed protocol until an administrator records an evidence-backed resolution; a version on another lineage is not held. | `test_E04…`; journey step 5 | The resolution rule is this software's; nobody independent reviewed a resolution. |
| 6 | Exact duplicate review work is grouped: packets with the same claim version, financial contract and known source roots share one task and one reservation, across a restart, with every contributor kept; a different contract stays separate. | `test_C01_C02…`, `test_C08…`, `test_C01_intake…`; journey step 3 | Exact identity only. A duplicate or a shared source is not evidence of plagiarism or misconduct, and one source behind many packets is one root, not corroboration. |
| 7 | Concurrent reviewers cannot reserve the same minutes; a budget cut below recorded use shows an overrun instead of rewriting consumption. | `test_C05_concurrent…` (+ a variant with the check disabled, which the test detects), `test_C06_C09…`; `measurements.json` (0 violations in 200- and 600-operation runs) | One machine, seeded synthetic operations. The queue comparison is a **simulation**: no human productivity, error or fairness result exists (COM-12 open). |
| 8 | A practitioner cannot obtain the comparison before its attempt is committed — not on 31 requested URLs (pages, help, static, journal, dataset, claim and as-of views, exports, error pages, another session, a direct request), and not in an export; afterwards it opens once, labelled "not ground truth". | `tests/test_v8_mod_prc_web.py` (`test_P02…`, `test_P02_X11_sensitivity…`, `test_P03…` ×2); journey step 4 | Boundary = the HTTP server for a `practice` principal. Outside it: the host administrator, the curator, an administrator principal, outside help, a public answer. Records cannot prove authorship, comprehension or improved ability. **No human study; human benefit PENDING.** |
| 9 | A module packet verifies in a fresh workspace: events re-hash into the journal, views recompute, a signer the receiver never enrolled stays UNKNOWN_SIGNER, nothing is imported, a forged view or an unrevealed answer is rejected, and a truncated journal is detected against a separately held checkpoint. | `test_X08_P06…`, `test_S08…`; journey step 6 | Integrity, not truth. Offline, current authorization is UNKNOWN. A checkpoint inside the same packet proves nothing. |
| 10 | Existing data and behaviour are unchanged: the seven-step journey, the availability correction, the real-source replay and older export verification pass on the expanded candidate. | full suite 356 passed; fixtures journey 7/7 and real-source journey 7/7 from both installed artifacts | The real-source issuer remains RETROSPECTIVE and NOT ELIGIBLE. |

## Established technique · proposed integration · demonstrated advantage

| | |
|---|---|
| Established (prior art, not invented here) | Least-privilege capabilities; preconfigured trust roots with digest-bound signed statements (the SLSA verification pattern — no conformance is claimed); Landlock, seccomp and bubblewrap; dependency-scoped regression selection; exact de-duplication, quotas and leases; attempt-before-reference practice. |
| Proposed YUCLAW contribution | The **connection**: one canonical claim/version/source identity and one append-only journal under approval, change-sensitive review, duplicate-aware allocation, practice and fresh-workspace verification, with corrections preserved as history. |
| Demonstrated here | Software behaviour only, on fictional data, by automated fixtures. |

## Connected workflow versus the same components used separately (controlled, same fixtures)

| Measure | Separate prototype components (older preview; **a weaker baseline**, labelled as such) | Connected workbench (this candidate) |
|---|---|---|
| Identity links | free-text ids inside standalone JSON documents; an unknown claim or source id is accepted | every module id is checked against the workspace: unknown claim, version digest or source → refused (`E_UNKNOWN_CLAIM`, `E_CLAIM_VERSION`, `E_UNKNOWN_SOURCE`) |
| Stale links under a controlled source correction | not detected (no shared history) | COM group flagged `SOURCE_TIME_CORRECTED`; PRC session annotated; original attempt and results unchanged (`test_P07…`) |
| Fresh-workspace reconstruction | not possible (planner snapshots, no journal) | packet re-hashed and recomputed (`test_X08_P06…`, journey step 6) |
| **Manual re-entry (unfavourable result, disclosed)** | ids typed into JSON by hand | **still required**: in the integrated journey a person types or pastes an identifier 11 times (bundle sha256 and evidence digests for an approval, SHD decision id for COM intake, claim ids for packets and tasks, source ids, an evaluation id for a resolution). The integration validates identifiers; it does not yet remove the typing. |

No superlative, novelty, patentability, state-of-the-art security or user-benefit claim follows from any of this, from the
number of tests, or from the length of the order.
