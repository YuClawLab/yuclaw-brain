# 8.0.1 — consolidated defect repair (tracked record)

Research and education only. Not investment advice. **8.0.1 is NOT PUBLISHED.** This record describes a release CANDIDATE on the validation branch `codex/v8.0.1-quality-fixes`:
`release_authorized=false`; any allocation for it is a new PROPOSED document; the 8.0.0 acceptance and authorization belong to 8.0.0 only.
Gate #15 stays REMOVED_BY_OWNER / NOT_REQUIRED (never PASSED); human benefit PENDING; owner walkthrough NOT_PERFORMED_OPTIONAL;
no independent security review is claimed.

Candidate SOURCE commit `4e792c97d3027ce3948d97dfd4aec652463f9096` (tree `56fc1c2846fa50dd4ad4be3dc6d9e5f4c19ad9ab`). The commit that adds this record changes no packaged path.

## Review items → work items

Two private reviews of the published 8.0.0 raised 17 identifiers. They map to **13 work items — 10 reproduced defect themes and 3
compatibility / editorial decisions**; duplicates are merged, never counted twice. Reproductions are minimal synthetic ones that run from
this tree (`dispositions.json` has the reproduction column).

| Item | Original ids | Kind | Change | Regression | Remaining limit |
|---|---|---|---|---|---|
| C01 | YUC-001 | confirmed defect (P1) | v3/web/lab_prose.py derives every numerical, directional and sample-size sentence from the rigor dictionary the tables use (= the bundle's `expected`) and the bundle's identity; the generator uses it; the staged page was restaged from the public bundle (no database) | tests/test_lab_prose_derived.py (positive, negative, zero, unreliable, unavailable; table and prose move together; restage touches nothing else; staged page == bundle) | statistics themselves unchanged; after every merge of main the staged page must be restaged (the nightly render does it from the same numbers) |
| C02 | YUC-002 | confirmed defect (P1) | backend unavailable → exit 3 + message (+ JSON status); queried-zero-rows → exit 0 and says it was queried; bundled fixture → stated; API route answers 503 instead of cascade:null | tests/test_cli_backend_states.py (unreachable socket, refused TCP, disposable PostgreSQL node with zero rows and with a real event, fixture, JSON and text, canary password never printed) | build_cascade() keeps its optional-extra contract for the research response and the demo |
| C03 | YUC-003 | confirmed defect (P2) | only the expected connection failure is handled (exit 3, message, JSON status); bad arguments stay 2; internal errors propagate; why/replay/replay-lab help state their three different contracts; YUCLAW_CORPUS=snapshot untouched | tests/test_cli_backend_states.py; tests/test_v8_transcript_snapshot_mode.py (unchanged, still passes) | a reachable database without the expected tables is an internal failure and still shows its error |
| C04 | P8-03 | confirmed defect (P2) | one refusal path for every POST route: HTTP 409 'Operation already recorded' page (escaped reason, nothing written, next step); genuine chain damage / torn tail keep their fail-closed pages | tests/test_v8_op_conflict_http.py (real HTTP: identical retry, conflict on upload and on a form, restart with a fresh CSRF token, expired token, journal bytes unchanged, server survives, exactly-one-event) | a browser cannot keep a chosen file: the page says to choose it again |
| C05 | YUC-006, P8-01 | confirmed defect (adoption, P2) | `yuclaw workbench …` thin delegate; help, README, PyPI description and homepage explain the local workbench and SHD/EVO/COM/PRC in plain words with the tested route; website = research-content site, workbench = local | tests/test_v8_workbench_entry_and_selftest.py; tests/test_site_static_surfaces.py; clean-install checks from wheel and sdist | no hosted service or account system exists or was added |
| C06 | YUC-004, P8-02 | confirmed defect (presentation, P2) | G2 reads every shared-header page's <title> and 'YUCLAW vX.Y.Z' identity strings; the version step stages the homepage identity; historical files untouched | tests/test_release_manifest_titles.py; tests/test_site_static_surfaces.py | not a security finding; archived guides are not current pages |
| C07 | YUC-005 | confirmed defect (P2) | the maintained v8 guide (shipped in the package) is the primary link; the earlier PDFs stay at their advertised versionless addresses, labelled historical with their real length; no French content added | tests/test_site_static_surfaces.py; release-manifest rule B4 unchanged and green | the PDFs were not re-rendered; the current guide is English only |
| C08 | YUC-007, P8-05 | confirmed defect (stale wording) + corrected interpretation (identity was present) | installed scorer primary, 7.0.0 checkout labelled historical; code / v1 / v2 candidate / item sets / registration / validation stated separately; echo control computed on the release's own items; canonical hash vs raw file SHA-256; weekly lineage from repository history (872 on 2026-09-11 and 750 on 2026-09-18 are two weekly sets) | tests/test_evidencebench_page.py; existing rubric contracts (tests/test_public_qa_20260915.py) unchanged | v2 stays a candidate: not registered, no v2 item set, nothing validated; releases older than the repository history are not asserted |
| C09 | YUC-008 | confirmed defect (P2) | visible labels, real sort buttons in <th scope=col> with aria-sort, polite live count, zero-result row, reset, stable tie-break, no inline handlers; staged page restaged from the public data file | tests/test_explorer_accessibility.py (static everywhere; keyboard/focus/aria-sort/zero-result/reset/deep link/390 px/mouse in a real browser where a driver is installed — locally and in the hosted package job) | automated checks are not a WCAG or screen-reader certification |
| C10 | YUC-009, P8-04 | confirmed defect (P2) | packaged bounded self-check in a temporary fictional workspace with the real server, fixtures' recorded results, export + verification, tamper and conflict refusals, fail-closed journal, restricted worker or verified CLOSED SHD route; --require-isolation fails instead of skipping; --dev keeps the repository tests | tests/test_v8_workbench_entry_and_selftest.py; clean-install runs both entry forms from wheel and sdist with --require-isolation | not the repository test suite; no browser; COM/PRC/EVO workflows not exercised by it |
| C11 | P8-06 | documented compatibility decision | retained; former_name_status says: deprecated compatibility alias, not a product claim, not positioning | tests/test_site_static_surfaces.py; claims lint and schema gates green | removing it would break the documented contract; not done |
| C12 | P8-07 | editorial improvement (partly overstated as a defect) | 8.0.1 notes, README section and homepage card state once: experimental, local, owner-operated; modules activate nothing; no independent security review; no user study (human benefit PENDING); scoreboard referenced under its own as-of time; no outsider count restated | tests/test_site_static_surfaces.py; notes correspondence check | published 8.0.0 notes not rewritten |
| C13 | P8-08 | owner-requested editorial consistency | 8.0.1 change list and CHANGELOG entry use 'Built in Canada'; nothing implies government endorsement, funding, data residency or an audited guarantee | tests/test_site_static_surfaces.py | the historical 8.0.0 changelog entry is unchanged |

Additional findings made while repairing (not review items, not counted): **X01** — REPAIRED in this patch (owner's final order of 2026-09-21). Found while repairing: the public /coverage.json was the 2026-09-15 instance while the published homepage, Explorer and Why surfaces named the 2026-09-18 instance (as-of time + source sha256). Root cause: cron/refresh_v3_pages.sh rewrites docs/coverage.json on every run (v3.web.coverage_public) and renders those surfaces from it, but its `git add` list did not name the file; the nightly gates read the working tree, so they stayed green while every refresh commit was inconsistent in a fresh checkout (check_coverage_consistency: 285 findings on a clean clone of main 46832138 and of the first candidate 5f1f0422; OK on the production working copy). Repair: the file is in the refresh's staging block; tools/deploy_verify.py waits for it like every other artifact; docs/coverage.json of this tree is the 2026-09-18 file, reproduced byte for byte by the unmodified generator from the source instance whose sha256 the 81 committed surfaces name (no value typed, no check changed). Regression: tests/test_refresh_staging_fresh_checkout.py executes the script's marked staging block verbatim in a disposable repository (real generator, real commit, real clone, unmodified gate, real scoring universe), proves the original list fails in the clone while its working tree is green, and checks this checkout's own committed site. Limits: the surface renderers need the research node and are stood in by writers that bind through coverage_public.read(); the fix reaches the public endpoint only when this patch is on main — until then each refresh of main repeats the omission, and a candidate that merges such a refresh restages the file the same way. packaging.json and the scorecards in this directory describe the verification build of the first candidate source 4e792c97 and are kept as recorded; the sdist of the final candidate differs from that build in docs/ (this file's subject and the 8.0.1 change list). **X02** — the README's 'v7 — check → reproduce …' output block was recorded on the 7.0.0 candidate and had been relabelled by each version step; the sentence now says where it was recorded. **I01** — INTEGRATION of 2026-09-22 (owner's bounded recovery order): the nightly refresh af5a9c10 (2026-09-21 23:00 UTC) was merged into the patch branch without force; the six conflicting generated pages were re-derived with the tracked tools (homepage fragments, Explorer template, Lab prose, EvidenceBench, version strings), coverage.json was restaged from that refresh's own artifact through the unmodified generator, preview surfaces and the README transcript were regenerated, and a fresh clone of the merge passes every database-free nightly check. The release-state derivation now runs production-data checks in read-only sessions and the test suite on a disposable database it creates (tools/yuclaw_disposable_pg.py; regression tests/test_v8_preflight_db_routing.py). The u350 isolation write probe (gate 4) is NOT RUN in this candidate's preflight: it needs a write-capable production session the owner has not authorized; gate 4 stays RED until that separate decision. Nothing about the checks themselves changed.

## Verification of the candidate source

- Clean-install (`packaging.json`): one build under `.github/v8-validation-constraints.txt`; wheel and sdist installed fresh outside the
  checkout; version, the delegated `yuclaw workbench` entry, the installed selftest in both entry forms with `--require-isolation`
  (landlock), the exact README transcript in the one transcript environment (`YUCLAW_CORPUS=snapshot`; bundled snapshot
  `6b8509c5f3423590…`, generated 2026-08-06 — not live data), packaged documents equal the commit, the
  workbench journey 7/7, the real-source regression journey 7/7 and the four-module journey
  6/6 on the `landlock` worker, each from both artifacts; member inspection clean; twine check rc 0.
- Scorecards: `scorecard_fixtures.json`, `scorecard_mchp.json`, `scorecard_modules.json` — every adjudication and note in them was recorded
  by the automated journey runner as a simulated test action; there was no human review and no user study.
- bubblewrap, macOS and Windows isolation remain unverified; SHD admission stays closed there.
