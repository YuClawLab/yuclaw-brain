# YUCLAW dataset releases

Weekly snapshot tags (`dataset-YYYY-MM-DD`) freeze, at a commit:
- `docs/evidencebench/items.jsonl` + `meta.json` (items, keys, item-set hash)
- `docs/why/*.json` (79 Ground Truth anatomy documents)
- `docs/ledger/*.json` (per-day ledger roots)

Under the export rule: derived events, verified excerpts, and keys only —
no raw vendor OHLCV. Cite via CITATION.cff at the repo root; event-level
citations use event IDs. Research and education only — not investment
advice; research classifications, not recommendations.

| tag | items | item-set hash |
|---|---|---|
| dataset-2026-08-05 | 348 | 51c0b55b917a52ec… |
